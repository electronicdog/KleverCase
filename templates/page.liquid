<div class="grid">

  <div class="grid-item large--two-thirds push--large--one-sixth">

    <header class="page-header">
      <h2>{{ page.title }}</h2>
    </header>

{{ product.metafields.c_f.featured_something.value }}

{{ page.metafields.c_f.featured_something.value }}

    {% comment %}
      Regular page content goes here.
    {% endcomment %}
    <div class="rte">
      {{ page.content }}
    </div>

  </div>

</div>